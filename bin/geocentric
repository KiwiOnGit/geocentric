#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"
if [ -x "$REPO_ROOT/run.sh" ]; then
  exec "$REPO_ROOT/run.sh" "$@"
fi
exec python3 "$REPO_ROOT/geocentric/cli.py" "$@"
